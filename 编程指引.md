# BuildPlan Workspace Knowledge Tool V0.1
## Coding Specification

# 1. 项目目标

开发一个独立的、本地优先（Local-first）的：

**Workspace Knowledge Indexing & Retrieval Tool**

它未来作为 BuildPlan Main Agent 的一个 Tool 使用。

核心职责：

本地 Workspace
→ 文件扫描
→ 文件解析
→ 结构化 Chunk
→ Embedding
→ 本地持久化向量索引
→ Semantic Retrieval
→ 返回 Evidence

本模块不负责最终自然语言回答。

Main Agent 未来只需要调用：

index_workspace()

search_knowledge()

get_index_status()

本模块不得依赖任何特定 Agent/ReAct 框架。


# 2. 当前版本范围

V0.1 ONLY：

- Windows
- Python
- Local-first
- DOCX
- SQLite
- Local Embedding
- Local Vector Store
- Semantic Search

暂时禁止实现：

- PDF
- MinerU
- XLSX
- Image/OCR
- Main Agent
- ReAct
- LangGraph
- Multi-Agent
- Web Search
- Built-in Standards
- Historical Knowledge
- Reranker
- GraphRAG
- Cloud RAG

但架构必须允许未来加入这些 Parser 和 Knowledge Scope。


# 3. 最终工作流程

用户选择：

D:/Projects/Huizhou/

系统执行：

Workspace Scanner
→ 找到支持的文件
→ SQLite检查文件状态
→ DOCX Parser
→ ParsedDocument
→ Structure-aware Chunker
→ Embedding
→ Local Vector Store
→ SQLite更新索引状态

以后：

Query
→ Embedding
→ Vector Search
→ Top-K Evidence
→ RetrievalResult


# 4. Workspace目录

用户原始项目：

workspace/
├── file1.docx
├── file2.docx
└── ...

BuildPlan允许自动创建：

workspace/
├── file1.docx
├── file2.docx
└── .buildplan/
    ├── project.db
    └── knowledge/

不得：

- 修改用户原文件
- 上传用户文件
- 将Workspace内容发送到外部服务
- 索引.buildplan自身


# 5. Public API

必须提供三个与Agent框架无关的Python接口。


## 5.1 index_workspace

index_workspace(
    workspace_path: str
) -> IndexWorkspaceResult

职责：

扫描Workspace；
发现可支持文件；
检查SQLite状态；
对新增/修改文件建立索引；
删除已经不存在文件的旧索引；
返回本次索引结果。


## 5.2 search_knowledge

search_knowledge(
    query: str,
    scope: str = "workspace",
    top_k: int = 5,
    filters: dict | None = None
) -> RetrievalResult

V0.1只实现：

scope="workspace"

但API设计必须允许未来：

builtin
standards
personal_history
all


## 5.3 get_index_status

返回：

- Workspace
- 发现文件数
- 已索引文件数
- Skip文件数
- Failed文件数
- Chunk总数
- 最后索引时间
- 每个文件状态
- Error


# 6. SQLite

使用：

workspace/.buildplan/project.db

SQLite当前负责：

**Workspace文件台账**

至少建立files表：

id
relative_path
file_name
file_type
file_size
file_hash
modified_time
indexed
indexed_at
chunk_count
parser_version
error_message

relative_path必须唯一。


# 7. Incremental Indexing

第一次：

new file
→ parse
→ chunk
→ embedding
→ vector store
→ SQLite

第二次：

计算hash。

hash unchanged：
→ SKIP

hash changed：
→ 删除该document旧vectors
→ 重新parse
→ chunk
→ embedding
→ upsert
→ 更新SQLite

文件已经被删除：
→ 删除SQLite记录
→ 删除Vector Store对应Chunks

禁止每次启动都重新Embedding所有文件。


# 8. Parser Architecture

定义统一：

BaseParser

概念接口：

parse(path) -> ParsedDocument

V0.1实现：

DocxParser

未来允许：

PdfParser
ExcelParser
ImageParser
MarkdownParser

但本轮不要实现它们。


# 9. DOCX Parser

使用python-docx。

必须识别：

- Heading
- Paragraph
- Table

不要简单：

join(all_paragraphs)

应尽量保留Word原始文档结构。

统一输出：

ParsedDocument

包含：

document_id
file_name
relative_path
file_type
elements
metadata

DocumentElement至少支持：

heading
paragraph
table

Heading必须保存level。

V0.1不解析embedded images。

发现图片：

warning
→ continue

不能导致整个DOCX失败。


# 10. Chunking

采用：

Structure-aware Chunking
+
Size-based Recursive Splitting

优先根据Heading形成语义章节。

例如：

4 施工部署
→ 4.3 施工工艺
→ 4.3.3 基础工程
→ 4.3.3.1 PHC管桩施工

Chunk必须保存：

heading_path

例如：

[
 "4 施工部署",
 "4.3 施工工艺",
 "4.3.3 基础工程",
 "4.3.3.1 PHC管桩施工"
]

同一章节内容优先保持在一起。

只有结构块过长才进一步切分。

初始实验：

target ≈ 600 tokens
overlap ≈ 100 tokens

参数必须集中配置。

禁止散落硬编码。

如果V0.1暂时使用字符近似token，
必须在README说明。


# 11. Chunk Schema

每个Chunk至少：

chunk_id
document_id
chunk_index
text
embedding_text
heading_path
metadata

metadata：

workspace_id
document_id
file_name
relative_path
file_type
heading_path

embedding_text允许加入Heading Context，
以增强语义检索。

原始text必须保留。


# 12. Table

小型表格尽量作为完整语义单元。

不要随意把表格拆散。

应转换为适合Embedding的文本表达，
同时保留table类型metadata。


# 13. Embedding Layer

Embedding必须抽象：

BaseEmbeddingProvider

至少：

embed_text()
embed_texts()

V0.1：

优先使用：

- 本地运行
- 支持中文
- 支持英文
- 可批量Embedding
- 可替换

的成熟Embedding模型。

Embedding模型不得和MiMo/Main Agent绑定。

Main Agent LLM != Embedding Model。


# 14. Vector Store Layer

定义：

BaseVectorStore

至少支持：

upsert_chunks()
search()
delete_by_document()
count()
health_check()

V0.1选择适合Local-first的Vector Store。

所有数据必须本地持久化。

不得要求用户额外维护复杂服务器。

具体Vector Store实现不得泄漏到Public API。


# 15. Retrieval

search_knowledge流程：

query
→ query embedding
→ vector search
→ optional metadata filters
→ Top-K
→ RetrievalResult

RAG模块不调用LLM生成最终答案。

只返回Evidence。


# 16. RetrievalResult

统一返回BuildPlan自己的Schema：

query
scope
results[]

每个result：

chunk_id
text
score

source:
    document_id
    file_name
    relative_path
    heading_path

禁止Public API返回：

LangChain Document
Qdrant Point
第三方框架对象


# 17. LangChain

允许使用LangChain内部组件。

例如：

langchain-text-splitters

但是：

LangChain只能作为内部实现细节。

不得让BuildPlan Public API依赖LangChain。

如果只需要Text Splitter，
优先只安装对应子包。


# 18. 测试路线

严格按照以下顺序。


## Test 1 — Parser Test

使用：

RAG测试.docx

输出：

Heading
Paragraph
Table

人工检查：

内容是否完整；
顺序是否正确；
Heading是否识别。


## Test 2 — Chunk Test

打印前10个Chunks：

chunk_id
heading_path
length
text preview

人工检查：

是否存在断句严重问题；
不同章节是否错误混合；
Heading Context是否保留。


## Test 3 — Index Test

运行：

index_workspace(demo_workspace)

检查：

.buildplan/project.db
.buildplan/knowledge/

SQLite必须记录：

indexed=1
chunk_count
file_hash
indexed_at


## Test 4 — Manual Retrieval Test

不接LLM。

Terminal输入：

项目总工期是多少？

必须在Top-K找到：

305天

输入：

项目人力峰值是多少？

必须找到：

192人

同时显示：

score
file
heading_path
text


## Test 5 — Fixed Evaluation

建立约20道固定测试题：

tests/evaluation/rag_testset.json

每题：

id
question
expected_keywords
expected_section

覆盖：

项目概览
工期
日期
工序
资源
关键路径
里程碑
风险

实现：

evaluate_retrieval.py

输出：

Recall@1
Recall@3
Recall@5

以及失败题目。


## Test 6 — Incremental Index Test

同一文件不修改：

第二次index_workspace()
→ 必须SKIP。

修改DOCX：
→ 必须只重新索引该文件。

删除DOCX：
→ 必须删除对应SQLite记录和Vectors。


## Test 7 — Large Real-world Test

前六项通过后，
再使用大型真实施工策划DOCX测试：

- 大型文件
- 多层Heading
- Table
- 大量Chunks
- 性能
- 工程语义检索

不得一开始直接使用大型文件调试。


## Test 8 — LLM Demo

只有Retrieval测试通过以后才实现。

Question
→ search_knowledge()
→ Top-K
→ Temporary LLM
→ Answer

LLM Demo必须与核心Knowledge Tool分离。

不得把LLM写入search_knowledge()。


## Test 9 — Agent Integration

由Main Agent团队完成。

最终：

User
→ Main Agent
→ search_knowledge()
→ Evidence
→ Main Agent Answer


# 19. Testset原则

Testset必须固定。

不要每次用LLM重新生成答案。

修改：

Chunk size
Embedding
Overlap
Retriever

之后直接重新运行相同Testset。

这样可以客观比较RAG版本。


# 20. Logging

使用logging。

不要到处print。

记录：

workspace scan
file discovered
file skipped
file indexed
chunk count
embedding time
search time
errors

不得记录：

API Key
Secret
不必要的完整用户敏感内容


# 21. Config

统一配置：

chunk_size
chunk_overlap
embedding_model
vector_store_path
sqlite_path
top_k_default
supported_extensions
parser_version

禁止业务代码散落magic numbers。


# 22. Ignore Rules

index_workspace必须忽略：

.buildplan/
.git/
__pycache__/
.venv/
venv/
临时文件

未来预留：

.buildplanignore


# 23. 推荐目录

BP_RAG/
│
├── buildplan_knowledge/
│   ├── __init__.py
│   ├── api.py
│   ├── config.py
│   ├── schemas.py
│   ├── workspace.py
│   ├── database.py
│   │
│   ├── parsers/
│   │   ├── base.py
│   │   └── docx.py
│   │
│   ├── chunking/
│   │   └── chunker.py
│   │
│   ├── embeddings/
│   │   ├── base.py
│   │   └── local.py
│   │
│   ├── vectorstores/
│   │   ├── base.py
│   │   └── local.py
│   │
│   └── retrieval/
│       └── retriever.py
│
├── scripts/
│   ├── parse_demo.py
│   ├── chunk_demo.py
│   ├── index_demo.py
│   ├── search_demo.py
│   └── evaluate_retrieval.py
│
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── fixtures/
│   └── evaluation/
│
├── README.md
├── requirements.txt / pyproject.toml
└── .gitignore


# 24. Git与安全

Repository：

Xiaobai-source-web/BP_RAG

禁止commit：

.env
API Keys
用户真实Workspace缓存
.buildplan/
*.db
vector index
模型缓存
临时文件

测试fixture只有在确认允许进入Public GitHub时才能commit。

真实企业工程资料默认不得commit到Public Repository。


# 25. 开发流程

不要一次完成全部代码。

严格阶段开发。


Phase A

只检查：

- 当前目录
- Python版本
- Git状态
- 当前文件
- 当前依赖

然后提出：

1. 推荐Python版本
2. 推荐Embedding模型
3. 推荐Local Vector Store
4. 推荐依赖
5. 准备创建哪些文件
6. SQLite schema
7. Public API schema

此阶段：

不要大规模写代码。
不要安装依赖。
不要实现完整RAG。

等待用户确认。


Phase B

Schemas
SQLite
Workspace Scanner

测试通过后停止。


Phase C

DOCX Parser

运行Parser Demo。

输出真实解析结果。

等待人工确认。


Phase D

Chunker

打印前10个Chunks。

等待人工确认。


Phase E

Embedding
Vector Store
Indexing


Phase F

search_knowledge()

完成手动Retrieval Test。


Phase G

固定Testset
Evaluation


Phase H

Incremental Indexing完整测试。


Phase I

大型DOCX压力测试。


Phase J

可选Temporary LLM Demo。


# 26. Definition of Done

V0.1完成必须满足：

✓ DOCX Workspace扫描

✓ DOCX结构解析

✓ Structure-aware Chunking

✓ Local Embedding

✓ Local Persistent Vector Store

✓ SQLite文件台账

✓ Incremental Indexing

✓ search_knowledge()

✓ Top-K Evidence

✓ Metadata

✓ 固定Testset

✓ Recall@1/3/5 Evaluation

✓ README

✓ Tests

✓ Agent-independent Public API

✓ 完全Local-first

✓ 不修改用户原文件

✓ 不依赖Main Agent


# 27. 当前第一条指令

现在只执行 Phase A。

不要开始完整实现。

检查当前 BP_RAG Repository，
然后向我报告：

1. 当前Git状态
2. Python环境
3. 当前目录结构
4. 推荐依赖
5. 推荐Embedding方案
6. 推荐Local Vector Store
7. 推荐SQLite schema
8. 推荐Public API
9. 准备新增的文件
10. 你认为当前Specification存在的技术问题

等待我确认后，再开始Phase B。