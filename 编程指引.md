你现在需要在当前 VS Code Workspace 中开发 BuildPlan 项目的
“Workspace Knowledge Indexing & Retrieval Tool V0.1”。

请把它当成一个正式的软件工程模块开发，而不是一次性的 RAG Demo。

==================================================
一、项目背景
==================================================

BuildPlan 是一个面向施工进度计划编制、分析和动态调整的
Local-first AI Agent Workspace。

未来用户会：

1. 在本地安装 BuildPlan；
2. 选择一个本地工程项目文件夹作为 Workspace；
3. BuildPlan 扫描 Workspace 中的工程文件；
4. 对可支持的文件进行解析、切块和向量索引；
5. Main Agent 在需要项目知识时调用 search_knowledge()；
6. Knowledge Tool 从本地知识索引检索相关原始证据；
7. 将结构化检索结果返回 Main Agent；
8. Main Agent 根据证据回答问题或继续调用其他工具。

未来 Workspace 可能包含：

- DOCX
- PDF
- XLSX
- TXT
- Markdown
- JPG
- PNG
- 其他工程文件

但本次 V0.1：

【只实现 DOCX】

不要实现：
- PDF
- MinerU
- Excel
- 图片/OCR
- Main Agent
- ReAct
- Web UI
- Dify
- 联网搜索
- 多智能体
- 规范知识库

但是代码架构必须为这些未来功能预留扩展能力。

==================================================
二、核心架构原则
==================================================

本模块必须与 Main Agent 完全解耦。

不要假设 Agent 使用：
- LangChain Agent
- LangGraph
- 自研 ReAct
- 任何特定模型

Agent 最终只应该看到 BuildPlan 自己定义的 Python 接口。

LangChain 可以在 RAG 模块内部使用，
但禁止让 LangChain 的 Document、Retriever 等对象成为公共 API。

公共接口必须使用：
- Python primitive types
- dataclass 或
- Pydantic models

实现细节不得泄漏给调用者。

目标：

今天内部可以使用 LangChain，
未来即使删除 LangChain，
Main Agent 也不需要修改代码。

==================================================
三、V0.1必须提供的三个公共接口
==================================================

接口 1：

index_workspace(path)

作用：
扫描指定 Workspace，
发现 DOCX，
判断哪些文件需要建立/更新索引，
解析、Chunk、Embedding并写入本地向量存储。

概念接口：

index_workspace(
    workspace_path: str
) -> IndexWorkspaceResult


接口 2：

search_knowledge(
    query: str,
    scope: str = "workspace",
    top_k: int = 5,
    filters: dict | None = None
) -> RetrievalResult

作用：
根据自然语言 Query 搜索当前 Workspace 已建立的知识索引。

V0.1 scope 只真正实现：
workspace

但接口设计应允许未来增加：
builtin
personal_history
standards
all


接口 3：

get_index_status()

作用：
返回 Workspace 当前索引状态，包括：

- 文件总数
- 已索引文件数
- 跳过文件数
- 失败文件数
- 每个文件状态
- Chunk数量
- 最后索引时间
- 错误信息

==================================================
四、Workspace设计
==================================================

假设用户项目：

Huizhou/
├── 施工策划书.docx
├── 合同.docx
└── 其他文件

BuildPlan允许创建：

Huizhou/
├── 施工策划书.docx
├── 合同.docx
│
└── .buildplan/
    ├── project.db
    └── knowledge/

.buildplan 是 BuildPlan 内部工作目录。

不要修改用户原始 DOCX。

不要把解析结果写回原始文件。

==================================================
五、SQLite职责
==================================================

使用 SQLite 保存 Workspace 文件索引台账。

SQLite不是本模块的向量相似度搜索引擎。

至少创建 files 表。

建议字段：

id
file_path
relative_path
file_name
file_type
file_size
file_hash
modified_time
indexed
indexed_at
chunk_count
parser_version
error_message

要求：

file_path 或 relative_path 必须具备唯一约束，
防止重复插入同一个文件。

indexed：
0 = 未索引/索引失败
1 = 已成功索引

第一次扫描：

发现新文件
→ 计算 hash
→ 建立索引
→ SQLite记录

第二次扫描：

文件存在且 hash 未变化
→ skip

hash发生变化
→ 删除该文件旧Chunks
→ 重新解析
→ 重新Embedding
→ 更新记录

文件已经从 Workspace 删除
→ 删除SQLite对应状态
→ 删除向量库对应Chunks

实现 Incremental Indexing。

==================================================
六、DOCX Parser
==================================================

V0.1只处理 DOCX。

优先使用 python-docx。

不要简单使用：

"\n".join(all_paragraphs)

因为需要尽可能保留 Word 文档结构。

解析至少识别：

1. Heading
2. Paragraph
3. Table

定义统一中间结构，例如：

ParsedDocument

包含：

document_id
file_path
file_name
file_type
elements
metadata


DocumentElement 示例：

{
    "type": "heading",
    "text": "4.3 施工工艺方案",
    "level": 2
}

{
    "type": "paragraph",
    "text": "本工程……"
}

{
    "type": "table",
    "text": "...",
    "rows": [...]
}

注意：

V0.1暂时不处理DOCX embedded images。

但遇到图片：
不要报错导致整个文件解析失败。
记录 warning，然后继续处理文本。

==================================================
七、Chunking策略
==================================================

不要简单每N个字符硬切。

采用：

Structure-aware Chunking
+
Recursive Size Splitting

第一层：

根据 Word Heading 层级识别章节结构。

例如：

4 施工部署与施工工艺方案
→ 4.3 施工工艺方案
→ 4.3.3 基础工程施工
→ 4.3.3.1 PHC管桩施工
→ 正文

Chunk应该保留完整 heading hierarchy。

例如：

heading_path = [
    "4 施工部署与施工工艺方案",
    "4.3 施工工艺方案",
    "4.3.3 基础工程施工",
    "4.3.3.1 PHC管桩施工"
]

第二层：

如果某个结构块过长，
再使用 LangChain RecursiveCharacterTextSplitter
或等价方案进一步拆分。

初始实验参数：

target chunk size ≈ 600 tokens
overlap ≈ 100 tokens

如果当前实现不方便精确token计数，
可以先使用合理字符近似，
但把 chunk_size 和 overlap 放入 config，
禁止硬编码散落在业务代码里。

每个Chunk必须至少包含：

chunk_id
document_id
text
embedding_text
heading_path
metadata

metadata至少包含：

workspace_id
document_id
file_name
relative_path
file_type
heading_path
chunk_index

表格尽量作为完整语义单元处理。

不要把一张小表格任意拆成多个无意义文本块。

==================================================
八、Embedding
==================================================

Embedding层必须抽象。

定义类似：

BaseEmbeddingProvider

例如：

embed_text(text)
embed_texts(texts)

不要把具体Embedding模型写死到Chunker或Retriever中。

V0.1可以选择一个适合中英文语义检索的现成Embedding模型。

要求：

- 支持中文
- 支持英文
- 可以本地运行优先
- 模型可替换
- 批量Embedding
- 配置项集中管理

Main Agent使用MiMo与这里使用什么Embedding模型无关。

MiMo是生成/推理模型。
Embedding模型是检索模型。

两者必须解耦。

==================================================
九、Vector Store
==================================================

向量存储也必须抽象。

定义：

BaseVectorStore

至少支持：

upsert_chunks(...)
search(...)
delete_by_document(...)
count(...)
health_check(...)

不要让 search_knowledge() 直接依赖具体数据库API。

V0.1请选择适合Local-first开发和测试的方案。

如果使用 Qdrant：
优先考虑适合本地开发的模式，
不要要求用户单独部署复杂服务器。

如果经过依赖检查发现其他本地向量方案明显更适合V0.1，
可以提出理由，但不要擅自大幅改变架构。

==================================================
十、Retrieval
==================================================

实现：

search_knowledge()

流程：

query
↓
Embedding
↓
Vector Search
↓
Metadata Filter（如有）
↓
Top-K
↓
标准化RetrievalResult

不要在RAG Tool内部生成最终自然语言答案。

RAG Tool只负责：
Retrieval / Evidence

Main Agent以后负责：
Generation / Reasoning

RetrievalResult建议：

{
    "query": "...",
    "scope": "workspace",
    "results": [
        {
            "chunk_id": "...",
            "text": "...",
            "score": 0.91,
            "source": {
                "document_id": "...",
                "file_name": "...",
                "relative_path": "...",
                "heading_path": [...]
            }
        }
    ]
}

禁止直接向调用者返回：
LangChain Document
Qdrant Point
第三方框架对象

==================================================
十一、Metadata Filter
==================================================

V0.1即使只有一个Workspace，也要设计过滤能力。

未来可能过滤：

workspace_id
document_id
file_type
source_type
section

Vector Store层必须预留filter参数。

不要依赖关键词判断知识类型。

Filter由调用方显式传入。

==================================================
十二、测试文件
==================================================

第一阶段使用：

RAG测试.docx

这是一个小型、结构清晰的施工进度计划说明书。

先验证：
DOCX → Parser → Chunk → Embedding → Vector Store → Search

不要一开始测试大型文件。

第一份文件测试通过以后，
再测试：

中韩（惠州）产业园生活配套项目（一期）工程施工策划书(1).docx

该文件是大型真实工程施工策划文件，
用于测试：

- 大型DOCX解析
- 多层Heading
- 大量Paragraph
- Table
- Chunk数量
- 检索性能
- 真实工程语义检索
- 增量索引

==================================================
十三、建立固定Test Set
==================================================

为第一份 RAG测试.docx 建立：

tests/fixtures/
tests/evaluation/

创建固定测试问题集，例如：

rag_testset.json

格式类似：

[
  {
    "id": "Q001",
    "question": "项目总工期是多少？",
    "expected_keywords": ["305", "天"],
    "expected_section": "项目概览"
  },
  {
    "id": "Q002",
    "question": "项目计划开工日期是什么？",
    "expected_keywords": ["2026", "4", "1"],
    "expected_section": "项目概览"
  }
]

请根据实际测试文档建立约20个问题。

覆盖：

- 项目基本信息
- 工序
- 日期
- 工期
- 资源
- 关键路径
- 里程碑
- 风险

不要让LLM动态生成标准答案。

测试集必须固定，
以便以后修改Chunk策略、Embedding模型或Retriever后重新自动测试。

==================================================
十四、Retrieval Evaluation
==================================================

实现：

evaluate_retrieval.py

至少计算：

Top-1 Recall
Top-3 Recall
Top-5 Recall

定义：

如果正确证据/expected keywords
出现在返回的前K个Chunks之一，
则该问题Recall@K成功。

输出类似：

Questions: 20

Recall@1: 70%
Recall@3: 90%
Recall@5: 95%

并输出失败的问题：

Q007 FAILED
Question: ...
Top results:
1...
2...
3...

方便调试。

==================================================
十五、日志
==================================================

不要到处print。

使用Python logging。

至少记录：

INFO:
workspace scan
file discovered
file skipped
file indexed
chunk count
search time

WARNING:
unsupported file
image ignored
empty paragraph
empty document

ERROR:
parse failed
embedding failed
vector write failed

日志不要包含API Key等敏感信息。

==================================================
十六、配置
==================================================

建立统一config。

至少：

chunk_size
chunk_overlap
embedding_model
vector_store_path
sqlite_path
top_k_default
supported_extensions

不要把这些值散落在代码中。

==================================================
十七、推荐目录
==================================================

请优先采用类似：

buildplan_knowledge/
│
├── __init__.py
│
├── api.py
│
├── config.py
│
├── schemas.py
│
├── workspace.py
│
├── database.py
│
├── chunker.py
│
├── embeddings/
│   ├── __init__.py
│   ├── base.py
│   └── local.py
│
├── parsers/
│   ├── __init__.py
│   ├── base.py
│   └── docx.py
│
├── vectorstores/
│   ├── __init__.py
│   ├── base.py
│   └── local.py
│
└── retrieval/
    ├── __init__.py
    └── retriever.py

tests/
├── unit/
├── integration/
├── fixtures/
└── evaluation/

scripts/
├── index_demo.py
├── search_demo.py
└── evaluate_retrieval.py

如果当前Repository已有明确目录规范，
优先适配现有Repository，
不要为了符合上述示例强行重构整个项目。

==================================================
十八、依赖原则
==================================================

优先选择成熟、维护活跃的库。

可能包括：

python-docx
pydantic
langchain-text-splitters
sentence-transformers
qdrant-client

但：

在安装或写代码前，
先检查当前项目Python版本和已有requirements/pyproject。

不要破坏现有依赖。

不要盲目复制旧版LangChain教程接口。

如果使用LangChain，
尽量只引入真正需要的子包，
不要为了Text Splitter引入大量无关Agent依赖。

==================================================
十九、安全要求
==================================================

这是Local-first应用。

必须：

- 默认不上传Workspace文件
- 不把文件内容发送到外部服务
- Embedding V0.1优先本地运行
- 不记录API Key
- 不修改用户原文件
- 所有索引数据保存在本地
- 路径处理兼容Windows
- 对隐藏目录和.buildplan自身避免递归索引

index_workspace()必须自动忽略：

.buildplan/
.git/
__pycache__/
临时文件

避免把自己的索引重新索引进去。

==================================================
二十、暂时不要实现的功能
==================================================

禁止本轮主动扩展：

PDF/MinerU
XLSX
Image/OCR
Web Search
Agent
ReAct
LangGraph
Multi-Agent
Cloud Knowledge
Standards RAG
Reranker
Hybrid Search
GraphRAG
Knowledge Graph

这些属于后续版本。

本轮目标是把最小闭环做到正确、清晰、可测试。

==================================================
二十一、验收标准
==================================================

完成后必须能够：

1. 指定一个包含 RAG测试.docx 的Workspace；

2. 执行：
   index_workspace(path)

3. 自动创建：
   .buildplan/

4. SQLite记录该DOCX：
   hash
   indexed
   chunk_count
   indexed_at

5. 文档成功被解析为结构化Elements；

6. 生成Chunks并保留Heading信息；

7. 完成Embedding；

8. 写入本地Vector Store；

9. 执行：

search_knowledge(
    "项目总工期是多少？",
    top_k=5
)

能够在Top结果中检索到包含：
305天
的正确文段；

10. 执行：

search_knowledge(
    "项目的人力峰值是多少？",
    top_k=5
)

能够找到：
192人

11. 再次运行index_workspace()
如果DOCX没有变化：
必须Skip，
不能重新Embedding整个文件。

12. 修改DOCX后再次运行：
必须检测hash变化并只重新索引该文件。

13. 删除DOCX：
再次扫描后必须清除对应索引。

14. evaluate_retrieval.py可以自动跑固定测试集并输出Recall@1/3/5。

==================================================
二十二、工作方式
==================================================

不要一次性生成大量未经验证的代码。

按以下阶段工作：

Phase A
检查当前Repository和Python环境。
向我汇报：
- 当前目录
- Python版本
- 已有依赖
- 是否存在与RAG相关代码
- 推荐新增依赖
- 计划创建/修改哪些文件

在我确认前不要大规模修改代码。

Phase B
建立schemas + SQLite + Workspace Scanner。

完成后运行测试。

Phase C
实现DOCX Parser。

完成后使用RAG测试.docx打印ParsedDocument结果，
让我检查Heading/Paragraph/Table解析是否正确。

Phase D
实现Chunker。

输出前10个Chunks：
- text preview
- heading_path
- chunk length
- metadata

让我人工检查。

Phase E
实现Embedding + Vector Store。

Phase F
实现search_knowledge()。

先不要接LLM。
直接在Terminal打印Top-K证据。

Phase G
建立固定testset并运行Retrieval Evaluation。

Phase H
只有Retrieval达到可接受效果以后，
再增加一个独立的demo脚本接临时LLM，
验证：
Question
→ Retrieval
→ Context
→ LLM Answer

这个LLM demo不得进入Knowledge Tool核心接口。

==================================================
二十三、最终交付
==================================================

完成后请提供：

1. README
2. 架构说明
3. 安装命令
4. Demo运行方法
5. Tool API说明
6. SQLite Schema
7. Chunk Schema
8. RetrievalResult Schema
9. Testset
10. Evaluation结果
11. 当前限制
12. 后续扩展PDF/XLSX/Image的接口设计说明

特别说明：

这是未来会交给Main Agent直接调用的正式Tool，
不是一次性实验代码。

优先级：

正确性
> 接口稳定
> 可测试性
> Local-first隐私
> 可扩展性
> 开发速度
> 花哨功能